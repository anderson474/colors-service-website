//const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    colors: {
      // 'navy': '#1a237e', // Usando el azul marino que definiste en el prompt
      // 'gold': '#d4af37'
    }
  }
}
export default config